# Example configurations using [tf-ncl](https://github.com/tweag/tf-ncl)

## libvirt-simple
A single VM using the `dmacvicar/libvirt` terraform provider.

## libvirt-less-simple
A network of 3 libvirt VMs using the `dmacvicar/libvirt` terraform provider.
